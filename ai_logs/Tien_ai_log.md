
 Tuần | Tính năng / Nhiệm vụ kỹ thuật | Prompt sử dụng (Yêu cầu gửi AI) | Kết quả phản hồi từ AI | Vấn đề / Lỗi phát hiện | Cách khắc phục thực tế |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Tuần 1** | **Thiết kế OOP & Entity `FlashItem`** kế thừa `BaseEntity` | *"Tôi đang code dự án Java OOP quản lý Flash Sale bằng Console. Tôi cần tạo lớp FlashItem kế thừa từ lớp cha BaseEntity (chứa các trường id, createdAt, updatedAt). FlashItem cần có các thuộc tính: itemId, productId, eventId, productName, originalPrice, salePrice, initialStock, soldQty, và version (kiểu int dùng cho Optimistic Lock). Hãy viết code đầy đủ bao gồm constructor, getters/setters, ghi đè hàm getId() trả về itemId, hàm toCsvLine() và hàm static fromCsvLine(String line) để phục vụ cho việc đọc ghi file CSV."* | Sinh cấu trúc lớp `FlashItem` hoàn chỉnh với các thuộc tính và phương thức mapping CSV theo đúng yêu cầu kế thừa. | Thứ tự các trường dữ liệu trong hàm `toCsvLine()` và hàm `fromCsvLine()` không đồng bộ (ví dụ: ghi file theo thứ tự một kiểu nhưng đọc file parse dữ liệu lại theo thứ tự khác). | Chỉnh sửa lại thủ công để đảm bảo thứ tự các trường khớp 100% với header định nghĩa trong CSV: `itemId,productId,eventId,productName,originalPrice,salePrice,initialStock,soldQty,version`. |
| **Tuần 2** | **Thiết kế Entity `OrderDetail`** & Logic tự động tính toán | *"Hãy thiết kế lớp OrderDetail kế thừa BaseEntity trong Java. Lớp này có các trường: detailId, orderId, productId, quantity, unitPrice, subtotal. Thiết kế constructor và các hàm toCsvLine(), fromCsvLine(String line) cho lớp này. Hãy gợi ý cách đảm bảo subtotal luôn tự động cập nhật bằng quantity * unitPrice khi tạo mới đơn hàng để tránh sai lệch dữ liệu."* | Sinh code lớp `OrderDetail` và đề xuất gán trực tiếp `subtotal = quantity * unitPrice` trong constructor và các hàm setter liên quan. | Hàm `fromCsvLine()` sử dụng `split(",")` thông thường và parse chuỗi số trực tiếp. Khi dữ liệu CSV có khoảng trắng (ví dụ: `" 150000"`), chương trình ném ra lỗi `java.lang.NumberFormatException`. | Áp dụng hàm `.trim()` cho tất cả các phần tử chuỗi con trước khi parse số: `Integer.parseInt(parts[i].trim())`, đồng thời sử dụng `split(",", -1)` để giữ các cột rỗng. |
| **Tuần 3** | **Cài đặt Tầng Repository** (`FlashItemRepository` & `OrderDetailRepository`) | *"Tôi đã có lớp abstract Generic CsvRepository<T extends BaseEntity>. Bây giờ tôi cần viết hai lớp cụ thể là FlashItemRepository kế thừa CsvRepository<FlashItem> và OrderDetailRepository kế thừa CsvRepository<OrderDetail>. Hãy override phương thức protected T parseLine(String line) sử dụng hàm static fromCsvLine của các thực thể tương ứng."* | Sinh mã nguồn cho 2 lớp repository cụ thể, thực hiện override hàm `parseLine` thành công bằng cách gọi hàm static parse của Entity. | AI sinh constructor gọi `super()` nhưng truyền sai tên file CSV hoặc thiếu chỉ định thư mục gốc `"data"`, khiến file CSV bị tạo phân tán ở thư mục gốc của project thay vì gom vào thư mục `/data`. | Chỉnh sửa constructor gọi đúng `super("flash_items.csv", "header_chuẩn...")` để ép tệp tin CSV luôn được tạo và ghi nhận trong thư mục `/data`. |
Các công việc đã thực hiện:
Đọc tệp cấu trúc nhiệm vụ nhiem_vu_phan_cong.md để xác định chính xác các file và logic Thành viên 3 chịu trách nhiệm.
Tra cứu thực tế các lớp FlashItem.java, OrderDetail.java và FlashItemRepository.java trong thư mục code của dự án để đảm bảo nội dung prompt/lỗi phản ánh chính xác cấu trúc kỹ thuật (như việc kế thừa CsvRepository<T> và override parseLine).
