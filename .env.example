# The application reads process environment variables; this file is a template only.
SHOPEE_ADMIN_USERNAME=change-me
# Store an Argon2id PHC hash, never a plaintext password.
SHOPEE_ADMIN_PASSWORD_HASH=
